# Number platform not currently used
# Reserved for future settings (e.g. dynamic thresholds)
async def async_setup_entry(hass, config_entry, async_add_entities):
    """Set up the number platform."""
    pass
